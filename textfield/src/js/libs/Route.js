/*import authRoute from '../routes/auth'

const routes = [
    {
      path: '/api/auth',
      file: authRoute
    },
];

export default (app) => {
  routes.forEach((router) => app.use(router.path, router.file));
};
*/
